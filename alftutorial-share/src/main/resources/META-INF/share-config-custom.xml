<!-- Share configuration related to this particular Share module extension, such as:
     - Doclib action config
     - Form config
     - Aspect and Type config

    Note. Any configuration related to the whole Share.war webapp
           (i.e. not specific to this share extension) should be
            placed in the environment specific config:
             alfresco/tomcat/shared/classes/alfresco/web-extension/share-config-custom.xml file
     -->
<alfresco-config>

	<config evaluator="string-compare" condition="DocumentLibrary">
		<aspects>
			<visible>
				<aspect name="myc:uniqueid"/>
			</visible>
		</aspects>
	</config>
	
	<config evaluator="aspect" condition="myc:uniqueid">
		<forms>
			<form>
				<!-- This has been added to display the default DBID generated by Alfresco -->
				<field-visibility>
                    <show id="sys:node-dbid" force="true" />
            	</field-visibility>
            	<!-- This has been added to display the default "OOTB-"+DBID generated by Alfresco -->
            	<field-visibility>
                    <show id="myc:id" />
            	</field-visibility>
			</form>
		</forms>
	</config>
</alfresco-config>